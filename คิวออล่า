local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local REMOTE = ReplicatedStorage:WaitForChild("meleeEvent")
local RANGE = 20
local Enabled = false -- เริ่มปิด ต้องเรียกเปิดเอง

local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local RootPart = Character:FindFirstChild("HumanoidRootPart")

LocalPlayer.CharacterAdded:Connect(function(char)
    Character = char
    RootPart = char:WaitForChild("HumanoidRootPart")
end)

local function GetNearbyPlayers()
    local nearby = {}
    if not RootPart then return nearby end
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local targetRoot = player.Character:FindFirstChild("HumanoidRootPart")
            if targetRoot then
                local distance = (targetRoot.Position - RootPart.Position).Magnitude
                if distance <= RANGE then
                    table.insert(nearby, player) -- เก็บ Player object เพื่อ debug
                end
            end
        end
    end
    return nearby
end

-- ฟังก์ชันเปิด/ปิด
local function ToggleMelee()
    Enabled = not Enabled
    print("Melee:", Enabled and "ON" or "OFF")
end

-- Loop หลัก
RunService.Heartbeat:Connect(function()
    if not Enabled then return end
    if not RootPart then return end

    local targets = GetNearbyPlayers()
    if #targets == 0 then return end

    for _, target in pairs(targets) do
        print("เจอผู้เล่น:", target.Name) -- debug ว่าตัว Loop ทำงาน
        local success, err = pcall(function()
            REMOTE:FireServer(target) -- ต้องปรับ args ให้ตรงกับเกมจริง
        end)
        if not success then
            warn("Error firing meleeEvent:", err)
        end
    end
end)

-- ตัวอย่างเรียกเปิด
-- ToggleMelee()elee()
