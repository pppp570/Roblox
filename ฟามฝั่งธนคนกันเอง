-- SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- TARGET PATH
local base = workspace["\224\184\151\224\184\181\224\185\136\224\184\159\224\184\178\224\184\161"]

local machine1 =
	base.Model["Washing Machine"].Model.MachineBase

local machine2 =
	base.Model:GetChildren()[3]:GetChildren()[4]

--------------------------------------------------
-- GUI
--------------------------------------------------
local gui = Instance.new("ScreenGui")
gui.Name = "ฟาม"
gui.ResetOnSpawn = false
gui.Parent = player.PlayerGui

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.fromScale(0.18, 0.14)
frame.Position = UDim2.fromScale(0.4, 0.4)
frame.BackgroundColor3 = Color3.fromRGB(15,15,15)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true

-- มุมโค้ง
Instance.new("UICorner", frame).CornerRadius = UDim.new(0,16)

-- ขอบ RGB
local stroke = Instance.new("UIStroke", frame)
stroke.Thickness = 2
stroke.Color = Color3.fromRGB(255,0,0)

-- ไล่สีขอบ
task.spawn(function()
	local h = 0
	while task.wait() do
		h = (h + 1) % 360
		stroke.Color = Color3.fromHSV(h/360, 1, 1)
	end
end)

-- TITLE
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.fromScale(1,0.35)
title.BackgroundTransparency = 1
title.Text = "ฟาม"
title.Font = Enum.Font.GothamBold
title.TextScaled = true
title.TextColor3 = Color3.new(1,1,1)

-- BUTTON
local btn = Instance.new("TextButton", frame)
btn.Size = UDim2.fromScale(0.8,0.4)
btn.Position = UDim2.fromScale(0.1,0.5)
btn.BackgroundColor3 = Color3.fromRGB(30,30,30)
btn.Text = "OFF"
btn.Font = Enum.Font.GothamBold
btn.TextScaled = true
btn.TextColor3 = Color3.fromRGB(255,80,80)
btn.BorderSizePixel = 0
Instance.new("UICorner", btn).CornerRadius = UDim.new(0,12)

--------------------------------------------------
-- LOGIC
--------------------------------------------------
local Auto = false

btn.MouseButton1Click:Connect(function()
	Auto = not Auto
	if Auto then
		btn.Text = "ON"
		btn.TextColor3 = Color3.fromRGB(80,255,80)
	else
		btn.Text = "OFF"
		btn.TextColor3 = Color3.fromRGB(255,80,80)
	end
end)

local function firePrompt(obj)
	for _, v in pairs(obj:GetDescendants()) do
		if v:IsA("ProximityPrompt") then
			v.HoldDuration = 0
			fireproximityprompt(v)
		end
	end
end

RunService.RenderStepped:Connect(function()
	if Auto then
		pcall(function()
			firePrompt(machine1)
			firePrompt(machine2)
		end)
	end
end)
