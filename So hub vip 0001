local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
local Window = WindUI:CreateWindow({
    Title = "My Super Hub",
    Icon = "door-open",
    Author = "by .ftgs and .ftgs",
    User = {
        Enabled = true,
        Anonymous = false,
        Callback = function()
            print("clicked")
        end,
    },
})

Window:ToggleTransparency(true)

local Tab = Window:Tab({
    Title = "Aimbot",
    Icon = "bird",
    Locked = false,
})

--------------------------------------------------
-- SERVICES
--------------------------------------------------

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

--------------------------------------------------
-- ตัวแปรระบบ
--------------------------------------------------

local selectedModes = {}
local lockConnection = nil
local currentTarget = nil

local fovEnabled = false
local FOV_RADIUS = 150

--------------------------------------------------
-- FOV GUI
--------------------------------------------------

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "FOV_GUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = game:GetService("CoreGui")

local circle = Instance.new("Frame")
circle.Size = UDim2.fromOffset(FOV_RADIUS * 2, FOV_RADIUS * 2)
circle.AnchorPoint = Vector2.new(0.5, 0.5)
circle.Position = UDim2.fromScale(0.5, 0.5)
circle.BackgroundTransparency = 1
circle.Visible = false
circle.Parent = screenGui

local stroke = Instance.new("UIStroke")
stroke.Thickness = 2
stroke.Color = Color3.fromRGB(0, 255, 0)
stroke.Parent = circle

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1, 0)
corner.Parent = circle

RunService.RenderStepped:Connect(function()
    if fovEnabled then
        circle.Size = UDim2.fromOffset(FOV_RADIUS * 2, FOV_RADIUS * 2)
        circle.Position = UDim2.fromScale(0.5, 0.5)
    end
end)

--------------------------------------------------
-- ตรวจสอบมองเห็นจริง (กันกำแพง)
--------------------------------------------------

local function isVisible(part)
    local origin = Camera.CFrame.Position
    local direction = part.Position - origin

    local rayParams = RaycastParams.new()
    rayParams.FilterType = Enum.RaycastFilterType.Blacklist
    rayParams.FilterDescendantsInstances = {LocalPlayer.Character}
    rayParams.IgnoreWater = true

    local result = workspace:Raycast(origin, direction, rayParams)

    if result then
        return result.Instance:IsDescendantOf(part.Parent)
    end

    return true
end

--------------------------------------------------
-- ตรวจสอบว่าใช้ได้ไหม
--------------------------------------------------

local function isValidTarget(player, partName)
    if player == LocalPlayer then return false end
    if not player.Character then return false end

    local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
    local part = player.Character:FindFirstChild(partName)

    if not humanoid or humanoid.Health <= 0 then
        return false
    end

    if not part then
        return false
    end

    local screenPos, onScreen = Camera:WorldToViewportPoint(part.Position)

    if not onScreen or screenPos.Z <= 0 then
        return false
    end

    -- เช็ค FOV
    if fovEnabled then
        local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
        local distance = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude
        if distance > FOV_RADIUS then
            return false
        end
    end

    -- เช็คกำแพง
    if not isVisible(part) then
        return false
    end

    return true
end

--------------------------------------------------
-- หาเป้าหมายใกล้กลางจอที่สุด
--------------------------------------------------

local function getClosestTarget(partName)
    local closest = nil
    local shortestDistance = math.huge
    local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)

    for _, player in pairs(Players:GetPlayers()) do
        if isValidTarget(player, partName) then
            local part = player.Character[partName]
            local screenPos = Camera:WorldToViewportPoint(part.Position)
            local distance = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude

            if distance < shortestDistance then
                shortestDistance = distance
                closest = part
            end
        end
    end

    return closest
end

--------------------------------------------------
-- DROPDOWN
--------------------------------------------------

Tab:Dropdown({
    Title = "โหมดการล็อค",
    Desc = "เลือกว่าจะล็อคตัวหรือหัว",
    Values = {"ล็อคตัว","ล็อคหัว"},
    Multi = true,
    AllowNone = true,
    Callback = function(option)
        selectedModes = option
        print("เลือกโหมด: "..HttpService:JSONEncode(option))
    end
})

--------------------------------------------------
-- TOGGLE LOCK
--------------------------------------------------

local Toggle = Tab:Toggle({
    Title = "เปิด Aimbot",
    Desc = "ต้องเลือกโหมดก่อน",
    Default = false,
    Callback = function(state)

        if state and (#selectedModes == 0) then
            warn("กรุณาเลือกโหมดก่อน")
            Toggle:Set(false)
            return
        end

        if not state then
            if lockConnection then
                lockConnection:Disconnect()
                lockConnection = nil
            end
            currentTarget = nil
            return
        end

        lockConnection = RunService.RenderStepped:Connect(function()

            local partName = nil

            if table.find(selectedModes,"ล็อคหัว") then
                partName = "Head"
            elseif table.find(selectedModes,"ล็อคตัว") then
                partName = "HumanoidRootPart"
            end

            if not partName then return end

            currentTarget = getClosestTarget(partName)

            if currentTarget then
                local newCFrame = CFrame.new(
                    Camera.CFrame.Position,
                    currentTarget.Position
                )
                Camera.CFrame = Camera.CFrame:Lerp(newCFrame, 0.2)
            end

        end)
    end
})

--------------------------------------------------
-- TOGGLE FOV
--------------------------------------------------

Tab:Toggle({
    Title = "เปิดวง FOV",
    Desc = "แสดง/ซ่อนวงกลางจอ",
    Default = false,
    Callback = function(state)
        fovEnabled = state
        circle.Visible = state
    end
})
local Tab = Window:Tab({
    Title = "esp",
    Icon = "eye", -- optional
    Locked = false,
})
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local espEnabled = false

-- เก็บ Highlight ทั้งหมด
local espObjects = {}

--------------------------------------------------
-- สร้าง ESP
--------------------------------------------------
local function createESP(character)
	if not character then return end
	if character:FindFirstChild("ESP_Box") then return end
	
	local highlight = Instance.new("Highlight")
	highlight.Name = "ESP_Box"
	highlight.Adornee = character
	highlight.FillTransparency = 1 -- กล่องใส
	highlight.OutlineColor = Color3.fromRGB(255,0,0)
	highlight.OutlineTransparency = 0
	highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
	
	highlight.Parent = character
	table.insert(espObjects, highlight)
end

--------------------------------------------------
-- ลบ ESP ทั้งหมด
--------------------------------------------------
local function removeESP()
	for _, v in pairs(espObjects) do
		if v and v.Parent then
			v:Destroy()
		end
	end
	espObjects = {}
end

--------------------------------------------------
-- ใส่ ESP ให้ผู้เล่น
--------------------------------------------------
local function setupPlayer(player)
	if player == LocalPlayer then return end
	
	player.CharacterAdded:Connect(function(char)
		task.wait(1)
		if espEnabled then
			createESP(char)
		end
	end)
	
	if player.Character and espEnabled then
		createESP(player.Character)
	end
end

for _, player in pairs(Players:GetPlayers()) do
	setupPlayer(player)
end

Players.PlayerAdded:Connect(setupPlayer)

--------------------------------------------------
-- Toggle ของคุณ
--------------------------------------------------
local Toggle = Tab:Toggle({
Title = "ESP Box",
Desc = "เปิด / ปิด กล่องใสรอบตัวละคร",
Icon = "bird",
Type = "Checkbox",
Value = false,
Callback = function(state)
	espEnabled = state
	
	if state then
		for _, player in pairs(Players:GetPlayers()) do
			if player ~= LocalPlayer and player.Character then
				createESP(player.Character)
			end
		end
	else
		removeESP()
	end
	
	print("ESP:", state)
end
})
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local MAX_DISTANCE = 500

local nameTags = {}
local espEnabled = false -- สถานะเปิด/ปิด

--------------------------------------------------
-- สร้างป้ายชื่อเล็กๆ
--------------------------------------------------

local function createNameTag(player)
	if player == LocalPlayer then return end
	
	local function setupCharacter(character)
		local root = character:WaitForChild("HumanoidRootPart", 5)
		if not root then return end
		
		if nameTags[player] then
			nameTags[player]:Destroy()
		end
		
		local billboard = Instance.new("BillboardGui")
		billboard.Name = "SmallNameTag"
		billboard.Size = UDim2.new(0, 90, 0, 18)
		billboard.StudsOffset = Vector3.new(0, -2.5, 0)
		billboard.AlwaysOnTop = true
		billboard.Enabled = false
		billboard.Adornee = root
		billboard.Parent = root
		
		local label = Instance.new("TextLabel")
		label.Size = UDim2.new(1, 0, 1, 0)
		label.BackgroundTransparency = 1
		label.TextSize = 11
		label.Font = Enum.Font.GothamBold
		label.TextColor3 = Color3.fromRGB(255,255,255)
		label.TextStrokeTransparency = 0.4
		label.Text = "[" .. player.Name .. "]"
		label.Parent = billboard
		
		nameTags[player] = billboard
	end
	
	if player.Character then
		setupCharacter(player.Character)
	end
	
	player.CharacterAdded:Connect(setupCharacter)
end

--------------------------------------------------
-- ตรวจจับผู้เล่น
--------------------------------------------------

for _, player in pairs(Players:GetPlayers()) do
	createNameTag(player)
end

Players.PlayerAdded:Connect(createNameTag)

Players.PlayerRemoving:Connect(function(player)
	if nameTags[player] then
		nameTags[player]:Destroy()
		nameTags[player] = nil
	end
end)

--------------------------------------------------
-- เช็คระยะ
--------------------------------------------------

RunService.RenderStepped:Connect(function()
	if not espEnabled then return end
	if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then return end
	
	local myRoot = LocalPlayer.Character.HumanoidRootPart
	
	for player, gui in pairs(nameTags) do
		if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
			local targetRoot = player.Character.HumanoidRootPart
			local distance = (myRoot.Position - targetRoot.Position).Magnitude
			
			gui.Enabled = distance <= MAX_DISTANCE
		end
	end
end)

--------------------------------------------------
-- เชื่อมกับ Toggle ของคุณ
--------------------------------------------------

local Toggle = Tab:Toggle({
    Title = "Name ESP",
    Desc = "Show players within 500 studs",
    Icon = "bird",
    Type = "Checkbox",
    Value = false,
    Callback = function(state)
        espEnabled = state
        
        -- ถ้าปิด ให้ซ่อนทั้งหมดทันที
        if not state then
            for _, gui in pairs(nameTags) do
                gui.Enabled = false
            end
        end
    end
})
